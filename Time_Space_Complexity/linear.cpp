#include <iostream>
#include <vector>

void linear_time(std::vector<int> arr, int key) {
    int size = arr.size();
    for (int i = 0; i < size; i++) {
        arr[i] = 0;
    }
}

int main (int argc, char *argv[]) {
    return 0;
}
